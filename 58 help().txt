
help()

Python’la ilgili herhangi bir konuda yardýma ihtiyacýnýz olduðunda, internetten araþtýrma yaparak pek çok ayrýntýlý belgeye ulaþabilirsiniz. Ama eðer herhangi bir nesne hakkýnda hýzlý bir þekilde ve Ýngilizce olarak yardým almak isterseniz help() adlý özel bir fonksiyondan yararlanabilirsiniz.

Bu fonksiyonu iki farklý þekilde kullanýyoruz. Birinci yöntemde, etkileþimli kabuða help() yazýp Enter düðmesine basýyoruz:

>>> help()

Welcome to Python 3.3!  This is the interactive help utility.

If this is your first time using Python, you should definitely check out
the tutorial on the Internet at http://docs.python.org/3.3/tutorial/.

Enter the name of any module, keyword, or topic to get help on writing
Python programs and using Python modules.  To quit this help utility and
return to the interpreter, just type "quit".

To get a list of available modules, keywords, or topics, type "modules",
"keywords", or "topics".  Each module also comes with a one-line summary
of what it does; to list the modules whose summaries contain a given word
such as "spam", type "modules spam".

help>

Gördüðünüz gibi, Python bu komutu verdiðimizde özel bir yardým ekraný açýyor bize. Bu ekranda >>> yerine help> ifadesinin olduðuna dikkat edin. Mesela dir() fonksiyonu hakkýnda bilgi almak için help> ifadesinden hemen sonra, hiç boþluk býrakmadan, þu komutu verebiliriz:

help> dir

Bu komut bize þu çýktýyý veriyor:

Help on built-in function dir in module builtins:

dir(...)
    dir([object]) -> list of strings

    If called without an argument, return the names in the current scope.
    Else, return an alphabetized list of names comprising (some of) the attributes
    of the given object, and of attributes reachable from it.
    If the object supplies a method named __dir__, it will be used; otherwise
    the default dir() logic is used and returns:
      for a module object: the module's attributes.
      for a class object:  its attributes, and recursively the attributes of its bases.
      for any other object: its attributes, its class's attributes, and
      recursively the attributes of its class's base classes.

Gördüðünüz gibi, dir() fonksiyonunun ne iþe yaradýðý ve nasýl kullanýldýðý konusunda ayrýntýlý bir bilgi ediniyoruz. Bu arada, hakkýnda bilgi almak istediðimiz fonksiyonu parantezsiz yazdýðýmýza dikkat edin.

Örnek olmasý açýsýndan mesela bir de len() fonksiyonu hakkýnda bilgi edinelim:

help> len

Help on built-in function len in module builtins:

len(...)
    len(object) -> integer

    Return the number of items of a sequence or mapping.

‘help’ ekranýndan çýkmak için Enter düðmesine basabilir veya quit komutu verebilirsiniz.

En baþta da dediðimiz gibi Python’da etkileþimli kabuk üzerinde Ýngilizce yardým almak için iki farklý yöntem kullanabiliyoruz. Bu yöntemlerden ilkini yukarýda anlattýk. Ýkincisi ise doðrudan etkileþimli kabukta þu komutu kullanmaktýr: (Mesela dir() fonksiyonu hakkýnda yardým alalým...)

>>> help(dir)

Help on built-in function dir in module builtins:

dir(...)
    dir([object]) -> list of strings

    If called without an argument, return the names in the current scope.
    Else, return an alphabetized list of names comprising (some of) the attributes
    of the given object, and of attributes reachable from it.
    If the object supplies a method named __dir__, it will be used; otherwise
    the default dir() logic is used and returns:
      for a module object: the module's attributes.
      for a class object:  its attributes, and recursively the attributes of its bases.
      for any other object: its attributes, its class's attributes, and
      recursively the attributes of its class's base classes.

Gördüðünüz gibi, ‘help’ ekranýný açmadan, doðrudan etkileþimli kabuk üzerinden de help() fonksiyonunu herhangi bir fonksiyon gibi kullanýp, hakkýnda yardým almak istediðimiz nesneyi help() fonksiyonunun parantezleri içine parametre olarak yazabiliyoruz.

Böylece dir(), enumerate() ve help() adlý üç önemli fonksiyonu da geride býrakmýþ olduk. Dilerseniz þimdi karakter dizilerine dair birkaç ufak not düþelim.
